{
  "name": "momentum",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'no build'",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0"
  }
}
